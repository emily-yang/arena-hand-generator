import React, {Component} from 'react';

const Header = (props) => (
	<header className="header">
		<p><strong>Hello!</strong> This app reads your MtG Arena draft deck from a log file and generates sample hands for your deck testing purposes. </p>
	</header>
)

export default Header;